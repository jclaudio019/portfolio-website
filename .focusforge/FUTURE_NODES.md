# Future Nodes

Each node must include ID, title, summary, source, capture context, reason parked, dependencies, value/readiness/alignment/risk/effort, `status: PARKED`, and `eligible_for_execution: false`.
