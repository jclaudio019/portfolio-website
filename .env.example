# Public base URL for the deployed Cloudflare Worker.
# Leave blank during local development to use the CRA proxy on port 8787.
REACT_APP_WORKER_API_URL=https://your-worker.example.workers.dev

# Optional contact API. When blank, the contact form uses mailto.
REACT_APP_BACKEND_URL=
